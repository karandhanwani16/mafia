PORT=3001
MONGODB_URI=mongodb://localhost:27017/mafia
FRONTEND_URL=http://localhost:5173
NODE_ENV=development

# Set to false to disable API rate limiting (e.g. for local dev)
API_LIMITER_ENABLED=true

# When true, new rooms always get room code 000000 (for easy testing). Set VITE_TESTING_MODE=true in frontend .env to match.
TESTING_MODE=false

# Socket server (run separately) - used to emit game events
SOCKET_SERVER_URL=http://localhost:3002
INTERNAL_API_KEY=dev-internal-key
